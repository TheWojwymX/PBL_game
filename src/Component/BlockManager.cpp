#include "BlockManager.h"

BlockManager::BlockManager(int width, int depth, int height) : _width(width), _depth(depth), _height(height) {} // Constructor with parameters initializes width, depth, and height

void BlockManager::GenerateMap() {
}
